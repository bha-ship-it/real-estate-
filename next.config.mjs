/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  /* Next.js internal App Router handles both UI and /api, /auth route handlers directly */
};

export default nextConfig;
